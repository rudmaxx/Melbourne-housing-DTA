# Проєкт: прогноз цін на житло в Мельбурні (полегшена версія)

Навчальний проєкт від початку до кінця: ви берете реальний датасет, очищаєте його, досліджуєте, створюєте нові ознаки і будуєте модель, яка прогнозує ціну житла. Працюєте лише у двох інструментах: **Google Colab** (середовище для коду) і **GitHub** (місце зберігання даних та ноутбука).

Ця версія полегшена. Кожен крок докладно пояснено, а в блоках коду стоять підказки-коментарі: вони описують, що саме треба написати, а сам код ви набираєте самі.

Кілька базових понять наперед, щоб не загубитися:
- **DataFrame** - це таблиця з даними у Python (як аркуш Excel). Зазвичай її називають змінною `df`.
- **Рядок** - один об'єкт (одна квартира/будинок). **Стовпець** - одна характеристика (наприклад, ціна).
- **Пропуск (NaN)** - порожня клітинка, де немає значення.
- **Ознаки (features)** - вхідні дані, з яких модель робить висновок. **Ціль (target)** - те, що прогнозуємо (тут це `Price`).

---

## Мета

Відпрацювати повний цикл роботи аналітика даних:
очищення -> дослідження (EDA) -> створення ознак -> модель -> прогноз -> висновки.

Тип задачі: **регресія** (ціль - число, ціна).

> EDA (exploratory data analysis) - це "розвідка даних": ви дивитесь на дані очима і графіками, щоб зрозуміти їх, перш ніж навчати модель.

---

## Датасет

- Назва: **Melbourne Housing Snapshot**
- Джерело (Kaggle): `https://www.kaggle.com/datasets/dansbecker/melbourne-housing-snapshot`
- Файл: `melb_data.csv`
- Ціль для прогнозу: стовпець `Price` (ціна продажу).

Короткий зміст стовпців: характеристики об'єкта (кількість кімнат `Rooms`, тип житла `Type`, відстань до центру `Distance`, площа ділянки `Landsize`, рік побудови `YearBuilt`, координати тощо), а також район (`Suburb`, `Regionname`, `CouncilArea`), спосіб і дата продажу (`Method`, `Date`).

---

## Частина 1. Організація роботи (Colab + GitHub)

> Навіщо це. **GitHub** - це сайт, де зберігають файли проєкту (як хмара для коду). **Colab** - це безкоштовний онлайн-зошит, де можна писати і запускати Python прямо в браузері. Спочатку ви кладете дані на GitHub, а потім читаєте їх у Colab за спеціальним посиланням.

### 1.1. Створіть репозиторій

1. На GitHub натисніть **New repository**, дайте назву (наприклад, `melbourne-housing`), зробіть його публічним.
2. Додайте файл `README.md` (галочка "Add a README file" при створенні).

Рекомендована структура репозиторію:

```
melbourne-housing/
  data/            # сюди покладете датасет
    melb_data.csv
  notebooks/       # сюди збережете ноутбук з Colab
    solution.ipynb
  README.md        # опис проєкту, кроки, висновки
```

Папку всередині GitHub можна "створити", вказавши шлях у назві файлу при завантаженні (наприклад, `data/melb_data.csv`).

### 1.2. Завантажте датасет з Kaggle

1. Відкрийте сторінку датасета (посилання вище). Для скачування потрібен безкоштовний акаунт Kaggle.
2. Натисніть кнопку **Download** - отримаєте архів. Розпакуйте його і знайдіть `melb_data.csv`.

### 1.3. Додайте датасет у репозиторій

1. У репозиторії натисніть **Add file -> Upload files**.
2. Перетягніть `melb_data.csv`. Щоб він потрапив у папку `data/`, у полі імені вкажіть шлях `data/melb_data.csv` (або спершу створіть папку).
3. Натисніть **Commit changes**.

Зверніть увагу: GitHub приймає файли до 25 МБ через веб-завантаження; наш файл значно менший, тож проблем не буде.

### 1.4. Отримайте RAW-посилання

Щоб Colab міг прочитати файл напряму, потрібне **RAW-посилання** (на "сирий" вміст, а не на веб-сторінку GitHub):

1. Відкрийте `data/melb_data.csv` у репозиторії.
2. Натисніть кнопку **Raw**.
3. Скопіюйте URL з адресного рядка. Він має вигляд:
   `https://raw.githubusercontent.com/<ваш_логін>/<ваш_репозиторій>/main/data/melb_data.csv`

Чому саме RAW: звичайне посилання на GitHub веде на HTML-сторінку з оформленням, а RAW віддає чистий вміст файлу, який бібліотека читання CSV зрозуміє.

### 1.5. Робота в Colab і збереження назад у GitHub

1. Відкрийте `https://colab.research.google.com` -> новий ноутбук.
2. Дані підвантажуйте у пам'ять напряму з RAW-посилання (передайте URL у функцію читання CSV з pandas). Завантажувати файл у Colab вручну не потрібно.
3. Коли закінчите: **File -> Save a copy in GitHub**, оберіть свій репозиторій і папку `notebooks/`. Так ноутбук збережеться у проєкті.

---

## Частина 2. Завдання покроково

Нижче - кроки і на що звертати увагу. Біля кожного кроку є докладне пояснення і шаблон коду з підказками-коментарями. Код пишете самі.

Найперша комірка у ноутбуці - підключення інструментів (бібліотек). Наберіть:

```python
# import - це "підключити інструмент". "as" - дати йому коротке ім'я.
import numpy as np            # numpy: робота з числами
import pandas as pd           # pandas: таблиці (DataFrame)
import matplotlib.pyplot as plt   # matplotlib: графіки

# щоб результати щоразу були однакові, зафіксуйте випадковість
RANDOM_STATE = 42
```

### Крок 1. Завантаження даних

- Підвантажте CSV з RAW-посилання у DataFrame.
- Переконайтеся, що таблиця прочиталася (кількість рядків і стовпців виглядає правдоподібно).

**Пояснення:** CSV - це текстовий файл-таблиця, де значення розділені комами. pandas уміє прочитати його однією функцією і покласти у DataFrame. Після читання завжди перевіряйте розмір таблиці: якщо рядків чи стовпців підозріло мало - щось пішло не так із посиланням.

**Шаблон коду з підказками:**

```python
# 1) збережіть ваше RAW-посилання у змінну (у лапках)
RAW_URL = "..."

# 2) прочитайте CSV у таблицю df.
#    - у pandas є функція для читання CSV (її назва починається на read_)
#    - передайте їй RAW_URL
# df = ...

# 3) перевірте, що все завантажилось:
#    - надрукуйте df.shape  -> покаже (рядків, стовпців)
#    - викличте df.head()   -> покаже перші 5 рядків
# ...
```

### Крок 2. Перший огляд

Подивіться на дані з кількох боків і занотуйте проблеми:

- розмір таблиці, перші рядки, типи стовпців;
- описова статистика числових стовпців;
- кількість і відсоток пропусків у кожному стовпці.

На що звернути увагу:
- чи немає технічного стовпця-індексу, який потрапив у файл;
- які стовпці мають дуже багато пропусків;
- чи правильно визначилися типи (наприклад, дата може зчитатися як текст);
- чи немає у статистиці підозрілих мінімумів або максимумів.

**Пояснення:** Перш ніж щось виправляти, треба роздивитися дані. Корисні інструменти:
- `df.dtypes` - тип кожного стовпця: число (int/float), текст (object) чи дата. Якщо дата записана як текст - це треба буде виправити.
- `df.describe()` - для чисел показує середнє, мінімум, максимум тощо. Тут легко помітити неможливі значення (наприклад, площа = 0 або рік 1196).
- `df.isna()` позначає порожні клітинки; `df.isna().sum()` рахує їх по стовпцях, а `df.isna().mean()` дає частку (від 0 до 1, помножте на 100 для відсотків).

**Шаблон коду з підказками:**

```python
# 1) базовий огляд: надрукуйте розмір і перші рядки
#    - df.shape
#    - df.head()
# ...

# 2) типи стовпців
#    - надрукуйте df.dtypes
# ...

# 3) пропуски: зробіть таблицю "скільки пропусків у кожному стовпці у відсотках"
#    - підказка: візьміть df.isna().mean(), помножте на 100, округліть
#    - відсортуйте від найбільшого до найменшого, щоб одразу побачити проблемні стовпці
# ...

# 4) описова статистика чисел
#    - викличте df.describe()
#    - подивіться на рядки min і max: чи немає неможливих значень?
# ...
```

### Крок 3. Очищення

Це ключовий крок. Перевірте і за потреби виправте:

- **технічний стовпець-індекс** - якщо є, приберіть;
- **дата** - перетворіть текст на справжній тип дати; зверніть увагу на порядок день/місяць;
- **нулі, що насправді означають "немає даних"** - подумайте, чи можлива площа, що дорівнює 0; такі нулі краще вважати пропусками;
- **фізично неможливі значення** - перевірте діапазон року побудови (значення на кшталт 1196 - явна помилка);
- **дублікати** рядків;
- **стратегія щодо пропусків** - для стовпців з дуже високою часткою пропусків подумайте, чи варто їх взагалі використовувати; для решти сплануйте заповнення (числові - якоюсь типовою величиною, категорійні - найчастішим значенням). Важливо: заповнення варто робити так, щоб інформація з тестової вибірки не "просочувалася" у навчання.

**Пояснення:** "Брудні" дані псують модель, тож їх готують:
- **технічний стовпець-індекс** (часто `Unnamed: 0`) - це просто номери рядків, що випадково потрапили у файл; користі немає, приберіть.
- **дата як текст.** Поки дата - це текст, з неї не можна дістати рік чи місяць. Перетворіть її на тип дати. Увага: у цьому файлі формат день/місяць/рік, тож функції перетворення треба підказати, що день іде першим.
- **нулі-несправжні.** Площа житла не може дорівнювати 0 - це насправді "немає даних". Такі нулі краще замінити на пропуск (`np.nan`), щоб не вводити модель в оману.
- **неможливі значення.** Рік побудови 1196 - очевидна помилка вводу. Замініть надто старі роки на пропуск.
- **дублікати.** Однакові рядки рахуйте через `df.duplicated()`; якщо вони є, приберіть.
- **пропуски.** Якщо у стовпці пропусків понад половину, його важко надійно заповнити - можливо, краще не використовувати. Решту заповните пізніше (на Кроці 6) усередині конвеєра, щоб уникнути "витоку" (про це далі).

**Шаблон коду з підказками:**

```python
# зробіть копію, щоб не псувати оригінал
clean = df.copy()

# 1) приберіть технічний стовпець-індекс, якщо він є
#    - подивіться у список стовпців; знайдіть зайвий (схожий на номери рядків)
#    - приберіть його через clean.drop(columns=[...])
# ...

# 2) перетворіть стовпець Date на тип дати
#    - використайте pd.to_datetime(...)
#    - не забудьте підказати, що день іде першим (аргумент dayfirst)
# ...

# 3) нулі-площі зробіть пропусками
#    - для відповідних стовпців (де нуль означає "немає даних")
#    - ідея: clean.loc[clean[стовпець] == 0, стовпець] = np.nan
# ...

# 4) неможливий рік побудови -> пропуск
#    - оберіть розумну нижню межу року (наприклад, нижче неї значення - помилка)
#    - присвойте таким клітинкам np.nan
# ...

# 5) перевірте дублікати
#    - надрукуйте clean.duplicated().sum()
#    - якщо більше 0 - приберіть через clean.drop_duplicates()
# ...

# 6) гляньте розмір після чищення: clean.shape
# ...
```

### Крок 4. Дослідження (EDA)

Мета - зрозуміти дані перед моделюванням. Подивіться:

- розподіл цілі `Price` (чи він симетричний, чи скошений; що це означає для моделі);
- як ціна залежить від типу житла та району;
- зв'язок числових ознак із ціною (кореляції);
- чи немає пар ознак, які майже дублюють одна одну (підказка: порівняйте `Rooms` і `Bedroom2`).

Робіть прості графіки (гістограма, boxplot, scatter) і формулюйте словами, що бачите.

**Пояснення:** Тут ви знайомитесь із даними через прості картинки:
- **гістограма** показує, як розподілені значення. Для ціни вона зазвичай "скошена" - багато дешевих об'єктів і довгий хвіст дорогих. Якщо взяти логарифм ціни, картинка стає рівнішою.
- **boxplot за категорією** (наприклад, за типом житла) показує, де ціни вищі, а де нижчі.
- **кореляція** - число від -1 до 1, що показує, наскільки два числові стовпці змінюються разом.

Про **кореляцію** докладніше, бо це важливий інструмент аналітика:
- Знак: `+` означає "разом зростають", `-` означає "коли одне більше, інше менше".
- Сила: чим ближче число до 1 або -1, тим сильніший лінійний зв'язок; близько 0 - зв'язку майже немає. Грубі орієнтири: понад 0.5 - помітний, 0.3-0.5 - помірний, менше 0.2 - слабкий.
- Навіщо: так ви бачите, які ознаки пов'язані з ціною (кандидати у важливі), і знаходите майже однакові ознаки (їх не варто тримати разом).
- Важливо: кореляція бачить лише **лінійні** зв'язки і **не означає причинність**.

**Шаблон коду з підказками:**

```python
# 1) розподіл ціни: побудуйте гістограму Price
#    - використайте plt.hist(clean["Price"], bins=50)
#    - додайте підписи й plt.show()
# ...

# 2) (необов'язково) гістограма логарифма ціни для порівняння
#    - підказка: np.log10(clean["Price"])
# ...

# 3) ціна за типом житла: boxplot
#    - підказка: clean.boxplot(column="Price", by="Type")
# ...

# 4) кореляція числових ознак ІЗ ЦІНОЮ:
#    - візьміть лише числові стовпці: num = clean.select_dtypes("number")
#    - порахуйте num.corr()
#    - візьміть стовпець ["Price"], приберіть сам Price (.drop("Price"))
#    - відсортуйте за модулем: .sort_values(key=abs, ascending=False)
#    - надрукуйте і прочитайте: що найсильніше пов'язане з ціною?
# ...

# 5) пошук майже-дублікатів: подивіться на повну матрицю кореляцій
#    - num.corr() (можна .round(2))
#    - знайдіть пари з дуже високим зв'язком (наприклад, Rooms і Bedroom2)
# ...
```

Після графіків і чисел **запишіть словами** 2-3 висновки: який тип житла дорожчий, як впливає відстань, які ознаки майже дублюються.

### Крок 5. Нові ознаки (feature engineering)

Створіть кілька осмислених ознак, наприклад:

- **вік будинку** на момент продажу (з року побудови і дати продажу);
- **частини дати** продажу (рік, місяць);
- **прапорці наявності** для стовпців з пропусками (сам факт пропуску буває інформативним);
- для району з дуже великою кількістю унікальних значень (`Suburb`) подумайте про кодування через частоту, а не через окремий стовпець на кожне значення.

Головне застереження: **не створюйте ознак з цілі**. Будь-що, пораховане з `Price` (наприклад, "ціна за кімнату"), - це витік: у момент реального прогнозу ціни ще немає.

**Пояснення:** Feature engineering - це створення нових корисних стовпців із наявних. Модель бачить лише ті ознаки, які ви їй дали, тож корисний сигнал варто зробити явним:
- **вік будинку** = рік продажу мінус рік побудови. Часто важливіший за сам рік побудови.
- **частини дати** (рік, місяць продажу) - бо ринок змінюється з часом.
- **прапорець наявності** - новий стовпець 0/1: чи було значення (1) чи був пропуск (0). Іноді сам факт пропуску щось означає.
- **частотне кодування** району: замість сотень стовпців на кожен район - один стовпець із тим, як часто цей район трапляється.

Найважливіше: ознака не повинна "підглядати" у ціль. "Ціна за кімнату" рахується з `Price`, якої в реальному прогнозі ще немає - це помилка (витік).

**Шаблон коду з підказками:**

```python
fe = clean.copy()

# 1) частини дати продажу
#    - рік: fe["sale_year"] = fe["Date"].dt.year
#    - місяць: за аналогією дістаньте .dt.month
# ...

# 2) вік будинку на момент продажу
#    - це рік продажу мінус рік побудови (YearBuilt)
#    - підстрахуйтесь: якщо вийшло від'ємне число, замініть на np.nan
# ...

# 3) прапорці наявності (пропуск як сигнал)
#    - для BuildingArea: fe["has_buildingarea"] = fe["BuildingArea"].notna().astype(int)
#    - зробіть так само has_yearbuilt для YearBuilt
# ...

# 4) частотне кодування району Suburb
#    - підказка: fe["suburb_freq"] = fe["Suburb"].map(fe["Suburb"].value_counts())
# ...

# 5) приберіть зайве:
#    - дубль ознаки (наприклад, Bedroom2, бо схожий на Rooms)
#    - надто розріджені стовпці й текст, який не кодуєте (Address, SellerG, Suburb тощо)
#    - використайте fe.drop(columns=[...])
# ...
```

### Крок 6. Підготовка до моделювання

- визначте ціль `y` (`Price`) і ознаки `X` (усе інше осмислене);
- розділіть ознаки на числові і категорійні;
- категорійні закодуйте (для номінальних - One-Hot; зважайте на високу кардинальність);
- зберіть усі кроки підготовки в єдиний конвеєр, щоб застосовувати їх однаково до навчальної і тестової частин;
- поділіть дані на train і test.

**Пояснення:** Модель приймає лише числа і не любить пропусків, тож дані треба підготувати:
- **X та y.** `y` - те, що прогнозуємо (`Price`). `X` - усі ознаки, з яких прогнозуємо (без `Price`).
- **train і test.** Дані ділять на дві частини: на навчальній (train) модель вчиться, на тестовій (test) ви чесно перевіряєте її на даних, яких вона не бачила. Це найважливіше правило: ніколи не оцінюйте на тих самих даних, на яких навчали.
- **кодування категорій.** Текст (наприклад, тип житла) перетворюють на числа. One-Hot робить окремий стовпець-прапорець для кожного значення. Якщо унікальних значень дуже багато (як у `Suburb`), One-Hot недоречний - тому ми зробили частотне кодування на Кроці 5.
- **заповнення пропусків** усередині конвеєра: числові - типовою величиною (медіаною), категорійні - найчастішим значенням.
- **конвеєр (Pipeline).** Це "склейка" всіх кроків підготовки і моделі в один об'єкт. Він гарантує, що підготовка вчиться лише на train і однаково застосовується до test (захист від "витоку").

**Шаблон коду з підказками:**

```python
from sklearn.model_selection import train_test_split
from sklearn.compose import ColumnTransformer
from sklearn.pipeline import Pipeline
from sklearn.impute import SimpleImputer          # заповнення пропусків
from sklearn.preprocessing import OneHotEncoder   # кодування категорій

# 1) ціль і ознаки
#    - y = ціна (стовпець Price)
#    - X = усе інше (приберіть Price)
# ...

# 2) поділіть стовпці на категорійні і числові
#    - cat_cols = [...]   # текстові: Type, Method, Regionname, CouncilArea
#    - num_cols = [...]   # решта числових (усе, що не в cat_cols)
# ...

# 3) опишіть підготовку через ColumnTransformer:
#    - для числових: SimpleImputer(strategy="median")
#    - для категорійних: спочатку SimpleImputer(strategy="most_frequent"),
#      потім OneHotEncoder(handle_unknown="ignore")
#    - підказка щодо структури:
# preprocess = ColumnTransformer([
#     ("num", ... , num_cols),
#     ("cat", Pipeline([("imp", ...), ("ohe", ...)]), cat_cols),
# ])
# ...

# 4) поділіть дані на train і test
#    - використайте train_test_split з test_size=0.2 і random_state=RANDOM_STATE
# ...
```

### Крок 7. Модель і прогноз

- спершу зробіть **базову лінію** (модель, що завжди передбачає середнє) - щоб було з чим порівнювати;
- навчіть просту модель (лінійна регресія) і складнішу (наприклад, випадковий ліс);
- оцініть на **тестовій** вибірці метриками регресії (середня абсолютна помилка, корінь із середньоквадратичної, коефіцієнт детермінації);
- подивіться на важливість ознак і поясніть словами, що впливає на ціну;
- зробіть прогноз для одного прикладу і порівняйте зі справжньою ціною.

**Пояснення:**
- **базова лінія** - найпростіша модель, що завжди каже середню ціну. Вона показує "мінімальний рівень"; нормальна модель має бути кращою за неї.
- **дві моделі.** Лінійна регресія - проста і зрозуміла. Випадковий ліс - складніша, зазвичай точніша на таких даних.
- **метрики** (рахуються на тесті):
  - MAE (середня абсолютна помилка) - на скільки доларів у середньому помиляємось;
  - RMSE - схоже на MAE, але сильніше карає великі промахи (це корінь із середньоквадратичної помилки);
  - R2 - яку частку розкиду ціни модель пояснює (1.0 - ідеально, 0 - не краще за середнє).
- **важливість ознак** (у лісу) показує, що найбільше впливає на ціну - це ваш головний висновок для людей.

**Шаблон коду з підказками:**

```python
from sklearn.dummy import DummyRegressor                 # базова лінія
from sklearn.linear_model import LinearRegression        # проста модель
from sklearn.ensemble import RandomForestRegressor       # складніша модель
from sklearn.metrics import mean_absolute_error, mean_squared_error, r2_score

# Зручно зробити невелику функцію, що навчає модель і друкує метрики:
def evaluate(model, name):
    # 1) склейте підготовку і модель у конвеєр:
    #    pipe = Pipeline([("prep", preprocess), ("model", model)])
    # 2) навчіть на ТРЕНУВАЛЬНИХ даних: pipe.fit(X_train, y_train)
    # 3) зробіть прогноз на ТЕСТОВИХ даних: pred = pipe.predict(X_test)
    # 4) порахуйте і надрукуйте MAE, RMSE (корінь із mean_squared_error), R2
    # 5) поверніть pipe (знадобиться далі)
    ...

# Викличте evaluate по черзі:
#  - DummyRegressor(strategy="mean")            -> базова лінія
#  - LinearRegression()                         -> проста модель
#  - RandomForestRegressor(n_estimators=200, random_state=RANDOM_STATE) -> ліс
# Порівняйте результати: яка модель найкраща і чому?
# ...

# Важливість ознак у лісу:
#  - у навченого лісу візьміть model.feature_importances_
#  - назви стовпців після One-Hot дістаньте через get_feature_names_out
#  - відсортуйте і подивіться топ ознак
# ...
```

### Крок 8. Висновки і README

- коротко опишіть у README: що за проєкт, які кроки очищення зробили, що показав аналіз, яка модель найкраща і яка її помилка;
- додайте 2-3 ідеї, як покращити результат далі.

**Пояснення:** README - це "обкладинка" проєкту, яку люди читають першою. Напишіть її простими словами, ніби пояснюєте другові.

**Шаблон коду з підказками (прогноз для прикладу):**

```python
# візьміть один рядок із тесту як "новий" об'єкт
#  - example = X_test.iloc[[0]]
# зробіть прогноз навченим лісом і порівняйте зі справжньою ціною:
#  - прогноз: pipe.predict(example)
#  - справжня ціна: y_test.iloc[0]
# ...
```

---

## Чек-лист "готово"

- [ ] репозиторій із структурою `data/`, `notebooks/`, `README.md`;
- [ ] датасет завантажений у репозиторій і читається в Colab через RAW;
- [ ] дані очищені (індекс, дата, нулі-пропуски, неможливі значення, пропуски);
- [ ] є щонайменше 3 графіки EDA з висновками;
- [ ] створено щонайменше 3 нові ознаки без витоку;
- [ ] підготовка зібрана в конвеєр, дані поділені на train/test;
- [ ] є базова лінія і дві моделі, оцінені на тесті;
- [ ] зроблено прогноз для прикладу;
- [ ] README з висновками;
- [ ] ноутбук збережений у репозиторій.

---

## Типові помилки

- оцінювати модель на тих самих даних, на яких вона навчалася (треба на тесті);
- кодувати район числами 0, 1, 2 (це нав'язує неіснуючий порядок);
- заповнювати пропуски, рахуючи статистику на всіх даних разом із тестом (витік);
- створити ознаку з `Price` і отримати фейкову точність (витік);
- залишити текстові стовпці чи пропуски і отримати помилку під час навчання;
- радіти результату, не порівнявши його з базовою лінією.

---

## Поради щодо README

Хороший README містить: назву і мету проєкту, посилання на джерело даних, короткий опис кроків (очищення, EDA, ознаки, модель), головні результати (метрики), 2-3 висновки людською мовою і список ідей для покращення. Це ваша "вітрина" проєкту - саме її переглядають насамперед.
